

function createNightButton() {
  const { body } = document;
  const nightButton = document.createElement('div');
  nightButton.className = 'night_mode';
  body.appendChild(nightButton);
  

}
//export default createNightButton;

